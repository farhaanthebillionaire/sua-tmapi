services:
  - type: web
    name: fastapi-task-manager
    env: python
    runtime: python-3.11.8
    buildCommand: python3.11 --version && pip3.11 install --upgrade pip && pip3.11 install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port ${PORT}
    envVars:
      - key: PORT
        value: 10000
      - key: SECRET_KEY
        generateValue: true
    healthCheckPath: /
